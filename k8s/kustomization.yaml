apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cdms-platform
resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml # copy from secret.yaml.example first, and keep it out of git
  - postgres.yaml
  - ai-platform.yaml
  - backend.yaml
  - web.yaml
  - ingress.yaml
